<Window x:Class="PageLeaf.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:PageLeaf.Behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PageLeaf.ViewModels"
        Title="設定"
        Width="680"
        Height="500"
        behaviors:WindowDragBehavior.IsEnabled="True"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        mc:Ignorable="d">
    <Border Style="{StaticResource SettingsWindowBorderStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    Style="{StaticResource SidebarPanelBorderStyle}">
                <StackPanel Margin="10,20">
                    <TextBlock Style="{StaticResource SettingsTitleTextStyle}" Text="設定" />

                    <RadioButton Content="エディタ"
                                 IsChecked="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Editor}}"
                                 Style="{StaticResource SidebarMenuItemStyle}"
                                 Tag="🖊️" />
                    <RadioButton Content="画像・ファイル"
                                 IsChecked="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Image}}"
                                 Style="{StaticResource SidebarMenuItemStyle}"
                                 Tag="🖼️" />
                    <RadioButton Content="表示 (コード)"
                                 IsChecked="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Code}}"
                                 Style="{StaticResource SidebarMenuItemStyle}"
                                 Tag="🎨" />
                </StackPanel>
            </Border>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="0"
                          Grid.Column="1"
                          Margin="25,25,25,10"
                          VerticalScrollBarVisibility="Auto">
                <Grid>
                    <!-- Editor Settings -->
                    <StackPanel Visibility="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Editor}}">
                        <Label Content="エディタ設定" Style="{StaticResource FirstSectionHeaderLabelStyle}" />

                        <CheckBox Content="タブの代わりにスペースを使用する"
                                  IsChecked="{Binding UseSpacesForIndent}"
                                  Style="{StaticResource FormCheckBoxStyle}" />

                        <Label Content="インデント幅 (スペース数)"
                               IsEnabled="{Binding UseSpacesForIndent}"
                               Style="{StaticResource FormLabelStyle}" />
                        <TextBox IsEnabled="{Binding UseSpacesForIndent}"
                                 Style="{StaticResource FormTextBoxSmallStyle}"
                                 Text="{Binding IndentSize}" />
                        <TextBlock Style="{StaticResource AnnotationTextStyle}" Text="※Tabキーを押した際に入力される空白の量を指定します。" />

                        <Label Content="フロントマター設定" Style="{StaticResource SectionHeaderLabelStyle}" />
                        <CheckBox Content="新規作成時にフロントマターを自動挿入する"
                                  IsChecked="{Binding AutoInsertFrontMatter}"
                                  Style="{StaticResource FormCheckBoxStyle}" />

                        <StackPanel IsEnabled="{Binding AutoInsertFrontMatter}">
                            <Label Content="追加で挿入するプロパティ" Style="{StaticResource FormLabelStyle}" />
                            <Border Style="{StaticResource PropertyGridContainerStyle}">
                                <StackPanel>
                                    <ItemsControl ItemTemplate="{StaticResource SettingsFrontMatterPropertyTemplate}" ItemsSource="{Binding DefaultFrontMatterProperties}" />
                                    <Button HorizontalAlignment="Left"
                                            Command="{Binding AddFrontMatterPropertyCommand}"
                                            Style="{StaticResource IconButtonStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Width="10"
                                                  Height="10"
                                                  Margin="0,0,5,0"
                                                  Data="{StaticResource AddIconGeometry}"
                                                  Fill="{StaticResource PrimaryActionBrush}"
                                                  Stretch="Uniform" />
                                            <TextBlock FontSize="11"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource PrimaryActionBrush}"
                                                       Text="プロパティを追加" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>
                            <TextBlock Margin="5,5,0,12"
                                       LineHeight="16"
                                       Style="{StaticResource AnnotationTextStyle}">
                                <Run Text="※使用可能なプレースホルダー:" />
                                <LineBreak />
                                <Run Text="  {Now} : 現在日時 (yyyy-MM-dd HH:mm:ss)" />
                                <LineBreak />
                                <Run Text="  {Date} : 現在日付 (yyyy-MM-dd)" />
                                <LineBreak />
                                <Run Text="  {Time} : 現在時刻 (HH:mm:ss)" />
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>

                    <!-- Image Settings -->
                    <StackPanel Visibility="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Image}}">
                        <Label Content="画像貼り付け設定" Style="{StaticResource FirstSectionHeaderLabelStyle}" />

                        <Label Content="保存先フォルダ (相対パス)" Style="{StaticResource FormLabelStyle}" />
                        <TextBox Style="{StaticResource FormTextBoxMediumStyle}" Text="{Binding ImageSaveDirectory}" />
                        <TextBlock Style="{StaticResource AnnotationTextStyle}" Text="※Markdownファイルからの相対パス。例: images" />

                        <Label Content="ファイル名テンプレート" Style="{StaticResource FormLabelStyle}" />
                        <TextBox Style="{StaticResource FormTextBoxMediumStyle}" Text="{Binding ImageFileNameTemplate}" />
                        <TextBlock Style="{StaticResource AnnotationTextStyle}" Text="※使用可能な変数: {Date}, {Time}, {FileName}" />
                    </StackPanel>

                    <!-- Code Highlight Settings -->
                    <StackPanel Visibility="{Binding CurrentCategory, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static vm:SettingsCategory.Code}}">
                        <Label Content="コードハイライト設定" Style="{StaticResource FirstSectionHeaderLabelStyle}" />

                        <Label Content="コードハイライト テーマ" Style="{StaticResource FormLabelStyle}" />
                        <ComboBox ItemsSource="{Binding AvailableThemes}"
                                  SelectedItem="{Binding SelectedCodeBlockTheme}"
                                  Style="{StaticResource FormComboBoxMediumStyle}" />
                        <TextBlock Style="{StaticResource AnnotationTextStyle}" Text="※コードブロックに適用される highlight.js のテーマを選択します。" />

                        <CheckBox Content="CSSエディタの設定でテーマを上書きする"
                                  IsChecked="{Binding UseCustomCodeBlockStyle}"
                                  Style="{StaticResource FormCheckBoxStyle}" />
                        <TextBlock Margin="25,-12,0,20"
                                   HorizontalAlignment="Left"
                                   LineHeight="15"
                                   Style="{StaticResource AnnotationTextStyle}"
                                   Text="※チェックすると、CSSエディタの「コード」タブでの設定がコードブロックに強制適用されます。" />
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="1"
                        Grid.Column="1"
                        Margin="0,0,25,20"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button Width="90"
                        Command="{Binding SaveCommand}"
                        Content="保存"
                        IsDefault="True"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <Button Width="90"
                        Margin="12,0,0,0"
                        Command="{Binding CancelCommand}"
                        Content="キャンセル"
                        IsCancel="True"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>