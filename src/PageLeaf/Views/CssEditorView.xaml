<UserControl x:Class="PageLeaf.Views.CssEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PageLeaf.Views"
             xmlns:controls="clr-namespace:PageLeaf.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <!-- Panel Toolbar -->
            <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="5">
                <Button Content="保存" HorizontalAlignment="Right" Command="{Binding SaveCssCommand}"/>
            </Border>

            <!-- Tab Container -->
            <TabControl>
                <!-- General Tab -->
                <TabItem Header="全体">
                    <StackPanel Margin="5">
                        <Label Content="文字色:"/>
                        <controls:ColorInputControl ColorText="{Binding BodyTextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="背景色:"/>
                        <controls:ColorInputControl ColorText="{Binding BodyBackgroundColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="フォントサイズ:"/>
                        <TextBox Text="{Binding BodyFontSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </TabItem>

                <!-- Headings Tab -->
                <TabItem Header="見出し">
                    <StackPanel Margin="5">
                        <Label Content="対象見出し:"/>
                        <ComboBox ItemsSource="{Binding AvailableHeadingLevels}"
                                  SelectedItem="{Binding SelectedHeadingLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        </ComboBox>
                        <Label Content="文字色:"/>
                        <controls:ColorInputControl ColorText="{Binding HeadingTextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="スタイル:"/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Content="太字" IsChecked="{Binding IsHeadingBold, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox Content="斜体" IsChecked="{Binding IsHeadingItalic, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox Content="下線" IsChecked="{Binding IsHeadingUnderline, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <CheckBox Content="取り消し線" IsChecked="{Binding IsHeadingStrikethrough, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <Label Content="フォントサイズ:"/>
                        <TextBox Text="{Binding HeadingFontSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="フォントファミリー:"/>
                        <TextBox Text="{Binding HeadingFontFamily, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <CheckBox Content="項番の自動採番"/>
                    </StackPanel>
                </TabItem>

                <!-- Quote Tab -->
                <TabItem Header="引用">
                    <StackPanel Margin="5">
                        <Label Content="文字色:"/>
                        <controls:ColorInputControl ColorText="{Binding QuoteTextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="背景色:"/>
                        <controls:ColorInputControl ColorText="{Binding QuoteBackgroundColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="左枠線 色:"/>
                        <controls:ColorInputControl ColorText="{Binding QuoteBorderColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="左枠線 太さ:"/>
                        <TextBox Text="{Binding QuoteBorderWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="左枠線 種類:"/>
                        <ComboBox Text="{Binding QuoteBorderStyle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ComboBoxItem Content="solid"/>
                            <ComboBoxItem Content="dotted"/>
                            <ComboBoxItem Content="dashed"/>
                        </ComboBox>
                    </StackPanel>
                </TabItem>

                <!-- List Tab -->
                <TabItem Header="リスト">
                    <StackPanel Margin="5">
                        <Label Content="マーカーの種類 (ul):"/>
                        <ComboBox Text="{Binding ListMarkerType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <ComboBoxItem Content="disc"/>
                            <ComboBoxItem Content="circle"/>
                            <ComboBoxItem Content="square"/>
                        </ComboBox>
                        <Label Content="インデント:"/>
                        <TextBox Text="{Binding ListIndent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </TabItem>

                <!-- Table Tab -->
                <TabItem Header="表">
                    <StackPanel Margin="5">
                        <Label Content="罫線 色:"/>
                        <controls:ColorInputControl ColorText="{Binding TableBorderColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="罫線 太さ:"/>
                        <TextBox/>
                        <Label Content="ヘッダー背景色:"/>
                        <controls:ColorInputControl ColorText="{Binding TableHeaderBackgroundColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="セル パディング:"/>
                        <TextBox/>
                    </StackPanel>
                </TabItem>

                <!-- Code Tab -->
                <TabItem Header="コード">
                    <StackPanel Margin="5">
                        <Label Content="文字色:"/>
                        <controls:ColorInputControl ColorText="{Binding CodeTextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="背景色:"/>
                        <controls:ColorInputControl ColorText="{Binding CodeBackgroundColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Content="フォントファミリー:"/>
                        <TextBox/>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>
</UserControl>
