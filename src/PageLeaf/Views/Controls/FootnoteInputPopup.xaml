<UserControl x:Class="PageLeaf.Views.Controls.FootnoteInputPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:PageLeaf.Behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="50"
             d:DesignWidth="400"
             mc:Ignorable="d">
    <Border MinWidth="300"
            MaxWidth="600"
            Padding="8"
            Background="{DynamicResource SurfaceBaseBrush}"
            BorderBrush="{DynamicResource OutlineBaseBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10"
                              Opacity="0.2"
                              ShadowDepth="2" />
        </Border.Effect>
        <Grid>
            <TextBox x:Name="InputTextBox"
                     VerticalContentAlignment="Center"
                     behaviors:FocusBehavior.IsFocused="True"
                     behaviors:KeyCommandBehavior.Command="{Binding CancelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     behaviors:KeyCommandBehavior.TargetKey="Escape"
                     Background="Transparent"
                     BorderThickness="0"
                     FontSize="14"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     KeyDown="InputTextBox_KeyDown"
                     LostFocus="InputTextBox_LostFocus" />
            <TextBlock Margin="5,0,0,0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       IsHitTestVisible="False"
                       Text="脚注の内容を入力...">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=InputTextBox}" Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Text, ElementName=InputTextBox}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
